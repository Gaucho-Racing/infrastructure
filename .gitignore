# Terraform
.terraform/
# .terraform.lock.hcl is intentionally NOT ignored — it pins provider
# versions across runs and should be committed alongside the root module.
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local
.DS_Store
.idea/
.vscode/
