# In-cluster handle for the gr-postgres EC2. Same instance as sentinel +
# mapache; isolated by DB name (vault).

apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: vault
spec:
  type: ExternalName
  externalName: gr-postgres.gauchoracing.com
