apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-prod
  namespace: postgres-prod
spec:
  podSelector:
    matchLabels:
      app: postgres-prod
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: 5432
