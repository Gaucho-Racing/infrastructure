apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-prod
  namespace: postgres-prod
spec:
  podSelector:
    matchLabels:
      app: postgres-prod
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchExpressions:
              - key: kubernetes.io/metadata.name
                operator: In
                values:
                  - postgres-prod
                  - cloudflared
                  - vault
                  - sentinel
                  - jiffy
                  - mapache
      ports:
        - protocol: TCP
          port: 5432
